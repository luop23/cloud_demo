spring:
  application:
    name: cloud-order-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848    #nacos作为配置中心地址
        file-extension: yaml   #指定yaml格式的配置

server:
  port: 7001  #端口号